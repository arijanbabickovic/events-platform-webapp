version: 0.2

phases:
  install:
    runtime-versions:
      ruby: latest
    commands:
      - echo Installing Bundler...
      - gem install bundler
      - bundle install

artifacts:
  base-directory: dist/events
  files:
    - '**/*'