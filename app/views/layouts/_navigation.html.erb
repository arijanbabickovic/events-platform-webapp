<nav class="navbar navbar-expand-lg navbar-dark navbar-custom sticky-top">
  <%= link_to "Events Platform", root_path, class: "navbar-brand" %>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <li class="nav-item">
        <%= link_to"Events", events_path, class: "nav-link" %>
      </li>
      <% if can? :create, Event %>
        <li class="nav-item">
          <%= link_to "Create new", new_event_path, class: "nav-link" %>
        </li>
      <% end %>
    </ul>
    <ul class="navbar-nav ml-auto">
      <% if user_signed_in? %>
        <% if can? :create, Event %>
          <li class="nav-item">
            <%= link_to "Admin dashboard", admin_dashboard_path, class: "nav-link" %>
          </li>
        <% end %>
          <li class="nav-item">
            <%= link_to"Your Account", user_path(current_user), class: "nav-link" %>
          </li>
          <li class="nav-item">
            <%= link_to"Log Out", destroy_user_session_path, class: "nav-link", method: :delete %>
          </li>
        <% else %>
          <li class="nav-item">
            <%= link_to"Sign Up", new_user_registration_path, class: "nav-link" %>
          </li>
          <li class="nav-item">
            <%= link_to"Log In", new_user_session_path, class: "nav-link" %>
          </li>
      <% end %>
    </ul>
  </div>
</nav>